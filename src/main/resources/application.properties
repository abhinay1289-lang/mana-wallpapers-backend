# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://db.roygltyllsnzhanbpswm.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=vardhan1289
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/api

# CORS Configuration
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# JWT Configuration
app.jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890}
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Supabase Storage Configuration
supabase.url=https://supabase.com/dashboard/project/roygltyllsnzhanbpswm/storage/buckets/mana-wallpapers
supabase.key=7d5b443031270b4fb6b9b17822b2ab3b
supabase.storage.bucket-name=mana-wallpapers

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Application Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:3000}
app.presigned-url.expiration=${PRESIGNED_URL_EXPIRATION:3600}

# Logging Configuration
logging.level.com.manawallpapers=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:DEBUG}